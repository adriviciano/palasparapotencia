---
import BaseLayout from "../layouts/BaseLayout.astro";
const base = import.meta.env.BASE_URL;
---

<style>
  .bola-rebotando {
    position: relative;
    width: 60px;
    animation: bote 1.2s infinite ease-in-out;
  }

  @keyframes bote {
    0%   { transform: translateY(0); }
    20%  { transform: translateY(-50px); }
    40%  { transform: translateY(0); }
    60%  { transform: translateY(-30px); }
    80%  { transform: translateY(0); }
    100% { transform: translateY(0); }
  }
</style>

<BaseLayout>
  <section style="text-align: center; padding: 4rem 1rem;">
    <h1 style="font-size: 4rem; color: var(--color-primario); margin-bottom: 1rem;">
      ¡404!
    </h1>
    <p style="font-size: 1.5rem; margin-bottom: 2rem;">
      La bola se ha ido fuera de la pista... Esta página no existe 😢
    </p>

    <!-- Animación -->
    <img src={`${base}/images/bola.png`} alt="Bola de pádel rebotando" class="bola-rebotando" />

    <p>
      <a href="/" style="color: var(--color-link); font-weight: bold;">
        Volver a jugar en la página principal
      </a>
    </p>
  </section>
</BaseLayout>
